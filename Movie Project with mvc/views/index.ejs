<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Landing Page</title>

    <style>
        /* Reset & base */
        *{box-sizing:border-box;margin:0;padding:0;font-family: 'Segoe UI', Roboto, Arial, sans-serif}
        body{background: linear-gradient(180deg,#f6f8ff 0%, #fffaf6 100%);color:#222;padding:28px}

        /* Header */
        header{display:flex;align-items:center;justify-content:space-between;margin-bottom:26px}
        .brand{display:flex;align-items:center;gap:14px}
        .logo{width:68px;height:68px;display:inline-grid;place-items:center;border-radius:14px;background:linear-gradient(135deg,#ff7a7a,#ffb27a);box-shadow:0 8px 22px rgba(0,0,0,0.12)}
        .logo svg{width:44px;height:44px;filter:drop-shadow(0 3px 8px rgba(0,0,0,0.12))}
        h1{font-size:22px;color:#111}
        .subtitle{font-size:13px;color:rgba(0,0,0,0.6)}

        /* Add button */
        .cta{display:inline-block;padding:12px 18px;border-radius:12px;text-decoration:none;font-weight:700;box-shadow:0 8px 22px rgba(99,102,241,0.18);transition:transform .12s ease, box-shadow .12s ease}
        .cta:active{transform:translateY(1px)}
        .cta.add{background: linear-gradient(90deg,#6d28d9,#3b82f6);color:white}

        /* Cards container - bigger cards */
        .cards{display:grid;grid-template-columns:repeat(auto-fill,minmax(300px,1fr));gap:20px;margin-top:12px}
        .card{background:linear-gradient(180deg, rgba(255,255,255,0.94), rgba(255,255,255,0.80));border-radius:16px;padding:16px;box-shadow:0 10px 30px rgba(13,38,59,0.07);overflow:hidden;border:1px solid rgba(13,38,59,0.045);display:flex;flex-direction:column;gap:12px;transition:transform .18s ease, box-shadow .18s ease}
        .card:hover{transform:translateY(-8px);box-shadow:0 22px 48px rgba(13,38,59,0.12)}

        .thumb{width:100%;aspect-ratio:16/9;object-fit:cover;border-radius:12px;border:1px solid rgba(0,0,0,0.05)}
        .meta{display:flex;align-items:center;justify-content:space-between;gap:12px}
        .title{font-weight:800;font-size:18px;color:#0f172a}
        .genre{font-size:14px;color:#475569;background:linear-gradient(90deg, rgba(59,130,246,0.08), rgba(99,102,241,0.06));padding:7px 10px;border-radius:10px}
        .rating{font-weight:800;color:#f59e0b;font-size:15px}

        .actions{display:flex;gap:10px}
        .actions a{padding:10px 12px;border-radius:10px;text-decoration:none;font-weight:700;border:1px solid rgba(15,23,42,0.06)}
        .btn-edit{background:linear-gradient(90deg,#ecfccb,#bbf7d0);color:#065f46}
        .btn-delete{background:linear-gradient(90deg,#ffe4e6,#ffd1d1);color:#991b1b}

        /* ID / footer small */
        .card small{color:rgba(15,23,42,0.45)}

        /* Empty state */
        .empty{padding:48px;border-radius:12px;text-align:center;color:rgba(0,0,0,0.55);background:linear-gradient(180deg,#ffffff,#fafafa);border:1px dashed rgba(0,0,0,0.06)}

        /* Responsive small screens */
        @media (max-width:520px){
            header{gap:10px;flex-direction:column;align-items:flex-start}
            .brand{gap:8px}
            .cta{width:100%;text-align:center}
            .cards{grid-template-columns:1fr}
        }
    </style>
</head>
<body>

    <header>
        <div class="brand">
            <div class="logo" aria-hidden="true">
                <!-- Movie clapper SVG icon -->
                <svg viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="Movies">
                    <rect x="2" y="7" width="20" height="13" rx="2" fill="white"/>
                    <path d="M2 7l4-4 4 4M10 7l4-4 4 4" stroke="#0f172a" stroke-width="1.2" stroke-linecap="round" stroke-linejoin="round"/>
                    <rect x="2" y="7" width="20" height="13" rx="2" stroke="rgba(0,0,0,0.06)"/>
                </svg>
            </div>
            <div>
                <h1>Landing Page</h1>
                <div class="subtitle">Cool colourful view of your movies — click a card to edit or remove</div>
            </div>
        </div>

        <!-- keep route same; styled anchor (functionality unchanged) -->
        <a href="/addMovies" class="cta add">+ Add Movie</a>
    </header>

    <!-- Cards view (keeps the same EJS data mapping and same edit/delete links => functionality unchanged) -->
    <% if (data && data.length > 0) { %>
        <div class="cards">
            <% data.map((e,i) => { %>
                <div class="card">
                    <img class="thumb" src="<%= e.image %>" alt="<%= e.title %> poster">
                    <div class="meta">
                        <div>
                            <div class="title"><%= e.title %></div>
                            <div class="genre"><%= e.genre %></div>
                        </div>
                        <div class="rating"><%= e.rating %> ⭐</div>
                    </div>

                    <p style="color:rgba(15,23,42,0.7);font-size:13px;margin-top:6px;line-height:1.3;"> <!-- optional short description slot if you have one -->
                        <!-- You can put movie short description here if available (no change to functionality). -->
                    </p>

                    <div style="margin-top:auto;display:flex;justify-content:space-between;align-items:center">
                        <div class="actions">
                            <a href="/editData?id=<%= e.id %>" class="btn-edit">Edit</a>
                            <a href="/deleteData?id=<%= e.id %>" class="btn-delete">Delete</a>
                        </div>
                    </div>
                </div>
            <% }) %>
        </div>
    <% } else { %>
        <div class="empty">No movies found. Click <a href="/addMovies" style="font-weight:700;color:#3b82f6;text-decoration:none">Add Movie</a> to create one.</div>
    <% } %>

</body>
</html>
