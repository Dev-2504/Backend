<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Library - Landing</title>
  <style>
    /* Inline CSS - modern, simple, responsive */
    :root{
      --bg:#f6f8fb; --card:#ffffff; --muted:#6b7280; --accent:#2563eb; --success:#10b981; --danger:#ef4444; --glass: rgba(15,23,42,0.04);
      --max-width:1100px;
    }
    *{box-sizing:border-box}
    body{margin:0;font-family:Inter, system-ui, -apple-system, 'Segoe UI', Roboto, 'Helvetica Neue', Arial;background:var(--bg);color:#0f172a}

    /* NAVBAR */
    .navbar{display:flex;align-items:center;justify-content:space-between;gap:16px;padding:16px 20px;background:linear-gradient(90deg,#0f172a, #0b1340);color:#fff}
    .brand{font-weight:700;font-size:18px;letter-spacing:0.3px}
    .nav-actions{display:flex;align-items:center;gap:12px}
    .btn{display:inline-flex;align-items:center;gap:8px;padding:8px 12px;border-radius:10px;border:none;cursor:pointer;font-weight:600;text-decoration:none}
    .btn-add{background:var(--accent);color:#fff;box-shadow:0 6px 18px rgba(37,99,235,0.12)}
    .btn-ghost{background:transparent;color:#fff;border:1px solid rgba(255,255,255,0.08)}

    /* Container */
    .container{max-width:var(--max-width);margin:26px auto;padding:0 18px}
    h1{margin:10px 0 6px 0;font-size:20px}
    p.lead{margin:0 0 18px 0;color:var(--muted)}

    /* Grid cards */
    .grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(260px,1fr));gap:18px}
    .card{background:var(--card);border-radius:12px;padding:14px;box-shadow:0 8px 24px rgba(12,18,30,0.06);display:flex;flex-direction:column;gap:10px;min-height:140px}
    .card .meta{display:flex;gap:12px;align-items:flex-start}
    .thumb{width:78px;height:100px;border-radius:8px;background:linear-gradient(180deg,#eaf2ff,#f3f7ff);display:flex;align-items:center;justify-content:center;color:var(--muted);font-weight:700}
    .info{flex:1}
    .title{margin:0;font-size:16px;font-weight:700}
    .author{margin:6px 0 0 0;color:var(--muted);font-size:13px}
    .category{font-size:12px;color:#475569;padding:6px 8px;border-radius:8px;background:var(--glass);display:inline-block;margin-top:8px}

    .card-footer{display:flex;justify-content:space-between;align-items:center;margin-top:auto}
    .actions{display:flex;gap:8px}
    .btn-edit{background:#0ea5e9;color:#fff;padding:8px 10px;border-radius:8px;text-decoration:none}
    .btn-delete{background:var(--danger);color:#fff;padding:8px 10px;border-radius:8px;text-decoration:none}

    /* Empty state */
    .empty{padding:40px;text-align:center;border-radius:12px;background:linear-gradient(180deg,#fff,#fbfdff);color:var(--muted);box-shadow:0 8px 24px rgba(12,18,30,0.04)}

    /* Responsive tweaks */
    @media (max-width:720px){
      .thumb{width:68px;height:86px}
      .grid{grid-template-columns:repeat(auto-fill,minmax(200px,1fr))}
      .navbar{padding:14px}
    }
  </style>
</head>
<body>
  <nav class="navbar">
    <div class="brand">My Library</div>
    <div class="nav-actions">
      <a href="/addForm" class="btn btn-add">+ Add Book</a>
    </div>
  </nav>

  <main class="container">
    <h1>Available Books</h1>
    <p class="lead">Your saved books are shown below. Click <strong>Add Book</strong> to create a new entry.</p>

    <% if (data && data.length) { %>
      <div class="grid">
        <% data.forEach(function(e){ %>
          <div class="card">
            <div class="meta">
              <div class="thumb">📚</div>
              <div class="info">
                <p class="title"><%= e.title || 'Untitled' %></p>
                <p class="author">By <%= e.author || 'Unknown' %></p>
                <div class="category"><%= e.category || 'General' %></div>
              </div>
            </div>

            <div class="card-footer">
              <div style="flex:1"></div>
              <div class="actions">
                <a href="/editData?id=<%= e.id %>" class="btn btn-edit" style="text-decoration:none;display:inline-block">Edit</a>
                <a href="/deleteData?id=<%= e.id %>" class="btn btn-delete" onclick="return confirm('Are you sure you want to delete this book?')" style="text-decoration:none;display:inline-block">Delete</a>
              </div>
            </div>
          </div>
        <% }) %>
      </div>
    <% } else { %>
      <div class="empty">No books yet. Click <a href="/addForm" style="color:var(--accent);font-weight:700;text-decoration:none">Add Book</a> to start adding.</div>
    <% } %>

  </main>
</body>
</html>
